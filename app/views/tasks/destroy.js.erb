for (let i=0 ; i < list.length; i++){
  let list = document.querySelectorAll("#list");
  let sibling = list[i].previousElementSibling.textContent;
  let ul = list[i].children[0];

  if (sibling  == "<%= @category.title %>"){
    ul.children[<%= @index_task %>].remove();
  }
}